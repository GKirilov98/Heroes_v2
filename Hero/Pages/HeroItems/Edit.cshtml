@page
@model Hero.Pages.HeroItems.EditModel

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>HeroItem</h4>
<hr />
<div class="row bg-blur">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="HeroItem.HeroItemId" />
            <div class="form-group">
                <label asp-for="HeroItem.HeroEId" class="control-label"></label>
                <select asp-for="HeroItem.HeroEId" class="form-control" asp-items="ViewBag.HeroEId"></select>
                <span asp-validation-for="HeroItem.HeroEId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="HeroItem.ItemId" class="control-label"></label>
                <select asp-for="HeroItem.ItemId" class="form-control" asp-items="ViewBag.ItemId"></select>
                <span asp-validation-for="HeroItem.ItemId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
